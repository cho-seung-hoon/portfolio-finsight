<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.finsight.backend.repository.mapper.InvTestMapper">
    <insert id="insert" parameterType="InvestmentProfileVO">
        INSERT INTO Investment_Profile (user_id, investment_profile_type, investment_profile_updated_at )
            VALUES (#{userId}, #{investmentProfileType}, #{investmentProfileUpdatedAt})
    </insert>
    <select id="selectInvestmentProfileTypeByUserId" parameterType="string" resultType="string">
        SELECT investment_profile_type
        FROM Investment_Profile  WHERE user_id = #{userId}
    </select>
    <select id="selectInvestmentProfileUpdatedAtByUserId" parameterType="string" resultType="string">
        SELECT investment_profile_updated_at
        FROM Investment_Profile  WHERE user_id = #{userId}
    </select>
</mapper>